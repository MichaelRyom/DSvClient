# This is a basic workflow that is manually triggered

name: Manual Build

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'DSvC Build workflow'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true
        # The data type of the input
        type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  release:
    name: Release - ${{ matrix.os-name }}
    strategy:
      fail-fast: false  # Don't fail fast to see all build issues
      matrix:
        include:
          - os-name: FreeBSD-x86_64
            runs-on: ubuntu-20.04
            target: x86_64-unknown-freebsd
            setup: |
              sudo apt-get update
              sudo apt-get install -y \
                gcc-multilib \
                pkg-config \
                gcc-freebsd-x86-64-linux-gnu \
                binutils-freebsd \
                libfreebsd-dev
              echo "CARGO_TARGET_X86_64_UNKNOWN_FREEBSD_LINKER=x86_64-freebsd-gcc" >> $GITHUB_ENV
              echo "CC_x86_64_unknown_freebsd=x86_64-freebsd-gcc" >> $GITHUB_ENV
              echo "RUSTFLAGS=-C target-feature=+crt-static" >> $GITHUB_ENV

          - os-name: Windows-x86_64
            runs-on: windows-latest
            target: x86_64-pc-windows-msvc

          - os-name: macOS-x86_64
            runs-on: macos-latest
            target: x86_64-apple-darwin

    runs-on: ${{ matrix.runs-on }}
    env:
      OPENSSL_STATIC: true
      PKG_CONFIG_ALLOW_CROSS: "1"
      OPENSSL_DIR: ${{ matrix.os-name == 'Windows-x86_64' && 'C:/vcpkg/installed/x64-windows-static' || '/usr/lib/ssl' }}

    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Build Environment
        run: ${{ matrix.setup }}
        shell: bash

      # Windows specific step
      - name: Install Dependencies (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          vcpkg integrate install
          vcpkg install openssl:x64-windows-static

      # macOS specific step
      - name: Install Dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          brew update
          brew install openssl@3
          echo "OPENSSL_ROOT_DIR=$(brew --prefix openssl@3)" >> $GITHUB_ENV
          echo "OPENSSL_LIB_DIR=$(brew --prefix openssl@3)/lib" >> $GITHUB_ENV
          echo "OPENSSL_INCLUDE_DIR=$(brew --prefix openssl@3)/include" >> $GITHUB_ENV

      # Linux specific step
      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev

      - name: Configure build environment
        run: |
          echo "RUSTFLAGS=-C target-feature=+crt-static" >> $GITHUB_ENV
          echo "OPENSSL_STATIC=1" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH=/usr/lib/ssl/pkgconfig" >> $GITHUB_ENV
        shell: bash

      - name: Create Changes.md
        run: |
          echo "# Changelog" > Changes.md
          echo "## v0.1.0" >> Changes.md
          echo "* Initial release" >> Changes.md
        shell: bash

      - name: Build Release
        uses: houseabsolute/actions-rust-cross@v0
        with:
          target: ${{ matrix.target }}
          args: "--locked --release"
          strip: true

      - name: Publish Release
        uses: houseabsolute/actions-rust-release@v0
        with:
          executable-name: vmware-downloader
          target: ${{ matrix.target }}
          changes-file: Changes.md

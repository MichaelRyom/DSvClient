# This is a basic workflow that is manually triggered

name: Manual workflow

# Controls when the action will run. Workflow runs when manually triggered using the UI
# or API.
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      name:
        # Friendly description to be shown in the UI instead of 'name'
        description: 'DSvC Build workflow'
        # Default value if no value is explicitly provided
        default: 'World'
        # Input has to be provided for the workflow to run
        required: true
        # The data type of the input
        type: string

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  release:
    name: Release - ${{ matrix.os-name }}
    strategy:
      matrix:
        os-name: [FreeBSD-x86_64, Linux-x86_64, Linux-aarch64, Linux-riscv64, Windows-x86_64, macOS-x86_64]
        include:
          - os-name: FreeBSD-x86_64
            runs-on: ubuntu-20.04
            target: x86_64-unknown-freebsd
            skip_tests: true

          - os-name: Linux-x86_64
            runs-on: ubuntu-20.04
            target: x86_64-unknown-linux-musl
            setup: |
              sudo apt-get update
              sudo apt-get install -y pkg-config libssl-dev

          - os-name: Linux-aarch64
            runs-on: ubuntu-20.04
            target: aarch64-unknown-linux-musl
            setup: |
              sudo apt-get update
              sudo apt-get install -y pkg-config libssl-dev

          - os-name: Linux-riscv64
            runs-on: ubuntu-20.04
            target: riscv64gc-unknown-linux-gnu
            setup: |
              sudo apt-get update
              sudo apt-get install -y pkg-config libssl-dev

          - os-name: Windows-x86_64
            runs-on: windows-latest
            target: x86_64-pc-windows-msvc
            setup: |
              vcpkg integrate install
              vcpkg install openssl:x64-windows-static

          - os-name: macOS-x86_64
            runs-on: macos-latest
            target: x86_64-apple-darwin
            setup: |
              brew install openssl@3

    runs-on: ${{ matrix.runs-on }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Install OpenSSL
        run: ${{ matrix.setup }}
        shell: bash
      
      - name: Set OpenSSL Env (Windows)
        if: matrix.os-name == 'Windows-x86_64'
        run: echo "OPENSSL_DIR=$env:VCPKG_ROOT\installed\x64-windows-static" >> $GITHUB_ENV
        shell: pwsh
        
      - name: Set OpenSSL Env (macOS)
        if: matrix.os-name == 'macOS-x86_64'
        run: echo "OPENSSL_DIR=$(brew --prefix openssl@3)" >> $GITHUB_ENV

      - name: Build binary
        uses: houseabsolute/actions-rust-cross@v0
        with:
          #command: ${{ matrix.include.command }}
          target: ${{ matrix.target }}
          args: "--locked --release"
          strip: true
      - name: Publish artifacts and release
        uses: houseabsolute/actions-rust-release@v0
        with:
          executable-name: ubi
          target: ${{ matrix.target }}
